# ミトの透過再生成 v2

2026-09-09。`codex/mito-assets` の既存作業を継続。内蔵gpt-imageで立ち絵・顔アイコン・7種類の動作（計60コマ）を再生成した。

- 立ち絵v1はアルファチャンネルがなく、市松模様が焼き込まれていた。旧アニメーションはマゼンタ背景からの色抜きだった。
- v2の立ち絵は人物設定から新規生成し、残り8種類はその立ち絵を参照した。実際の指示文は各版の `.prompt.txt` に保存。
- PNGは生成原本のまま保存。背景除去・拡大縮小などの画像加工は行っていない。
- 全9画像がRGBAで、完全透過の画素を確認。可視画素（alpha > 32）におけるマゼンタ寄りの画素は0。検出条件は R > 150、B > 120、G < R × 0.55、G < B × 0.55。数値は `pixel-v2-alpha-check.json`。
- 再生座標はアルファ128以上の画素で計測。行の空白を境界にし、足元の18pxの範囲から横基準点を設定。倍率は素材内で共通とし、コマごとの拡大縮小はしない。
- ブラウザで白・紺の背景、先頭・動作途中・最終コマを確認。市松模様の焼き込み、マゼンタの縁、隣接コマの混入は見られなかった。生成アニメーション特有の小物や表情の変化は残る。ユーザー確認により全9種類をv2採用とした。
- `node tools/character-assets.mjs --check` 成功。`node --test tools/character-assets.test.mjs` は5件成功。

`review-v2.html` で新版を一覧再生できる。比較のためv1を保持し、`adoptedAssets` は全9種類をv2に更新した。画廊では各動作でv2を優先表示する。
