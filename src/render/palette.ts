/**
 * 柄ごとの色。DOM に依存しないので、Worker が描く共有カード（src/ogp/）からも使う。
 * 色と図形の対応は原作と同じ組み合わせにしない（src/render/theme.ts の KIND_NAMES と対）。
 */
export const KIND_COLORS = [0xe0405a, 0x7ad33a, 0x4cc3e8, 0xf2d13b, 0xa25ad6, 0x3b62e0];
